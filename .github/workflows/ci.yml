name: CI

on:
  push:
    branches: [main, "agent/**"]
  pull_request:

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: shambalens
          POSTGRES_PASSWORD: shambalens
          POSTGRES_DB: shambalens_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U shambalens -d shambalens_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    defaults:
      run:
        working-directory: backend
    env:
      DATABASE_URL: postgresql+asyncpg://shambalens:shambalens@localhost:5432/shambalens_test
      DATABASE_URL_UNPOOLED: postgresql+asyncpg://shambalens:shambalens@localhost:5432/shambalens_test
      DEMO_MODE: "true"
      AI_PROVIDER: groq
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - run: alembic upgrade head
      - run: ruff check .
      - run: mypy app
      - run: pytest

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    # The Firebase web config is a public client identifier, not a secret — access is
    # enforced by Auth, Storage rules and App Check. CI only needs syntactically valid
    # placeholders so `lib/firebase.ts` can initialize at import time; without an apiKey
    # getAuth() throws auth/invalid-api-key and every test that imports it fails to load.
    env:
      NEXT_PUBLIC_FIREBASE_API_KEY: ci-placeholder-api-key
      NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: shambalens-ci.firebaseapp.com
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: shambalens-ci
      NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: shambalens-ci.appspot.com
      NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "0000000000"
      NEXT_PUBLIC_FIREBASE_APP_ID: "1:0000000000:web:0000000000000000000000"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e -- --project=chromium

  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cp .env.example .env
      - run: docker compose config
